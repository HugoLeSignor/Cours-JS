// 11.Table des multiplications
// Faire le tableau des multiplications de 0 à 9

for (let i = 0; i <= 9; i++) {
    console.log(`\nTable de ${i} :`);
    for (let j = 0; j <= 9; j++) {
        console.log(`${i} x ${j} = ${i * j}`);
    }
}
